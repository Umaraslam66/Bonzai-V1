# Resume handoff — sub-F validator v1.2 committed; 40-city re-validation is the gate (2026-06-05)

**Open here.** The v1.2 §8.3-termination relax + its drop-guard twin are committed and the full
local suite is green, but **the corpus has NOT been re-validated under v1.2** — that is the gating
task before any merge. This handoff is self-sufficient.

## 0. State

- Branch `phase-2-multiregion-extract`. **Stable anchor = the v1.2 CODE fix commit `87e2a49`** (it
  does not move as commits land on top). This handoff is a commit ON TOP of `87e2a49`, so the branch
  **tip** is whatever `git log --oneline -1` shows now (it changes every time this doc is edited — a
  doc cannot pin its own commit; that was the 5225c7a/e5fd0fe trap). **Deploy the whole BRANCH** (§3) —
  that brings `87e2a49` (the fix) + this handoff regardless of the tip sha. **Committed, NOT merged,
  NOT pushed.** Leonardo is at the OLD `5225c7a`; bring it to the current branch tip via git bundle (§3).
- Full local suite: **1109 passed**, 2 skipped (berlin local), 35 deselected (slow), 1 xfail.
- The investigation that motivated this: `reports/2026-06-05-batch2-subf-symmetry-fp-investigation.md`
  (read it — verdict, §8.3, census, source-trace, mechanism).

## 1. What v1.2 is (code fix commit `87e2a49`; this handoff is a later commit on the same branch)

The batch-2 sub-F BP7 symmetry failure on 8 cities (14 edges) was proven a **false positive**: a road
terminating exactly at an internal cell boundary (§8.3 touch-not-cross) is present one side only, so
one cell emits and the neighbour correctly emits nothing. Fix = **validator-only** (data sound):
- `_check_symmetry` + `_check_coverage` are road-presence-conditioned on `road_edge_presence`
  (sub-C `features.parquet`, `feature_class==0`, via the shared `encoder.endpoint_edge_direction`).
  One-sided emission ALLOWED iff neighbour has no road endpoint there; RAISES if it does
  (under-emission). `validate_cross_tile` gained `sub_c_region_dir` (pipeline + CLI + tests wired).
- `SUB_F_VALIDATOR_VERSION` 1.1→1.2 (VALIDATOR axis only; verdict-only, token bytes unchanged).
- **Drop-guard twin** `cfm.data.multiregion.lossless_clip.assert_lossless_clip`: the relax makes a
  real clip-DROP invisible to both legs, so this catches it by length conservation
  (`len(source∩bbox) == Σ per-cell fragment lengths`). **WIRING STATUS (read carefully):** this is a
  TESTED-BUT-UNWIRED helper — `assert_lossless_clip` is NOT called by any pipeline/validation path
  (only referenced in a docstring; verified `grep`). It does NOT run per-city on Leonardo. **For THIS
  corpus the source-trace gate (§2.3) is the SOLE drop check** — it is NOT a "second" backstop, it is
  the only one that runs. Wiring `assert_lossless_clip` as a recurring guard requires computing
  `len(source∩bbox)` at sub-C clip time (sub-C re-run) and is **DEFERRED to the regen era** alongside
  the §5 root fix. Do not let the green fixture imply a recurring guard that does not recur.

## 2. THE GATING TASK — re-validate under v1.2 (item 4)

Leonardo's processed corpus was built at the OLD validator (v1.1). Nothing has run under v1.2 yet.
After deploying the branch (§3 — confirm the fix `87e2a49` is in the deployed history):
1. **Re-derive + re-validate the processable cities** under v1.2 (re-run sub-F derive→validate; it
   re-stamps the v1.2 provenance and re-runs the cross-tile legs). The **8 symmetry cities**
   (bruges, manchester, eisenhuttenstadt, a_coruna, lodz, tychy, mannheim, krakow) must now PASS and
   get `_PHASE1_VALIDATED`. Re-validate the **12 already-validated + 5 canary** too for v1.2 version
   uniformity. (sub-C/D/E are unchanged — no re-extract; only sub-F re-derive, fast.)
2. **Per-city touch-as-cross population report** — run the census and record it in the close-out so
   the tolerated-spurious set stays on the books:
   `./.venv/bin/python scripts/multiregion/diagnostics/symmetry_probe.py --touch-census`
   (expected ~187 corpus-wide, 0.0064%, anomaly=0; ~65 silent in the passing cities).
3. **Source-trace gate — the SOLE drop check for this corpus** (the in-pipeline length guard is
   unwired, §1). Run `... symmetry_probe.py --city <c> --source-trace` for **every** processed city
   (not just the 8 — a drop can hide symmetrically in a "passing" city). **Merge is BLOCKED if ANY
   `len_in_MISS > 0`** (a real dropped crossing fragment). Expected: 0 everywhere. NOTE: `--source-trace`
   currently traces only the symmetry-DISAGREEMENT edges; to be a true corpus drop-gate it should trace
   ALL touch-as-cross edges (extend it, or accept it as a targeted check + document the gap).

**Do NOT merge if re-validation surfaces any symmetry/coverage RAISE that isn't a real defect, or any
`len_in_MISS > 0`.** The drop-guard is the twin that legitimizes the relax.

## 3. Deploy the branch to Leonardo (git bundle; branch is unpushed)

```
git bundle create /tmp/v12.bundle 5225c7a..phase-2-multiregion-extract
scp /tmp/v12.bundle leonardo:/tmp/v12.bundle
ssh leonardo 'cd /leonardo_work/AIFAC_P02_222/Bonzai-OSM && git pull --ff-only /tmp/v12.bundle phase-2-multiregion-extract'
# VERIFY the fix landed (sha-drift-proof — check the stable fix commit is in history,
# and the Leonardo tip equals your LOCAL git rev-parse HEAD):
ssh leonardo 'cd /leonardo_work/AIFAC_P02_222/Bonzai-OSM && git log --oneline | grep 87e2a49 && git rev-parse HEAD'
# expect: the 87e2a49 fix line present, and HEAD == your local `git rev-parse HEAD`.
```
Re-auth socket first if needed (laptop sleep): `step ssh login 'aslamumar016@gmail.com' --provisioner cineca-hpc` then `ssh -fN leonardo`. Memory [[reference_leonardo_claude_ssh_socket]].

## 4. Parked — untouched, separate problems (do NOT conflate with v1.2)

- **13 sub-C timeouts** (amsterdam, budapest, hamburg, helsinki, lisbon, lyon, madrid, paris, rome,
  rotterdam, valencia, vienna, warsaw): big-metro boxes don't finish sub-C in boost's 8h wall. A
  **box-sizing** problem, NOT a resubmit; longer wall may be unavailable on boost. The 40-city corpus
  is incomplete until these are resolved — a SEPARATE work item after v1.2 lands.
- **almere**: passed sub-F (0 symmetry disagreements); failed in **sub-G** validation; 42.7% of
  buildings dropped (alpha-drop band). **Guilty-until-proven** — check reclaimed-land-legit vs another
  data-loss bug against source the same way (source trace). Own bucket.
- **welwyn**: fetched (cache present), never processed.

## 5. sub-C touch-as-cross ROOT — file in known_issues (item 5, deferred)

The 14 FPs are downstream of a real sub-C bug: `_both_cells_present` (geom.py:557) tests
`per_cell_pieces` BEFORE the <0.01m sliver / 0-d discard, so a §8.3 touch (road ending at the edge)
is recorded as a crossing in `crossings.parquet` → sub-E marks the edge MINOR_ROAD → emission. File as:

> **spec-violating per §8.3** ("road ends exactly at boundary → 0 crossing records"), **TOLERATED
> under v1.2** (187/2.93M = 0.0064%, anomaly=0), **MUST be fixed at the next regen** — fix
> `_both_cells_present` to require positive-length presence. **When fixed, the corpus SHIFTS at the
> ~65 (of 187) symmetric edges** (they lose their bref). **Not "benign" — tolerated.** Un-skippable note.

## 6. Merge gate — TWO distinct questions; don't conflate them

**(A) Is the v1.2 FIX safe?** Proven by: re-validate under v1.2 (8 symmetry cities now pass) +
source-trace gate `len_in_MISS=0` everywhere + per-city touch-as-cross report + `reports/` close-out.
This certifies the validator change, NOT the corpus.

**(B) Is the CORPUS mergeable?** SEPARATE and NOT yet met. The corpus is **25/40 processable**
(13 timeouts + almere + welwyn unresolved, §4) and the **G4 three-part DoD is UNCHECKED**
(`build_g4_rollup.py`: ≥600M validated tokens + per-city floor + axis-coverage). "Re-validation green"
does NOT mean "ready to merge."

**PI DECISION REQUIRED (do not assume):** merge the **v1.2 validator fix on a known-incomplete corpus**
now (with the 13 timeouts / almere / welwyn / G4 DoD tracked as explicit follow-ups), **OR hold the
merge** until the corpus is complete (timeouts + almere + welwyn resolved) and G4 DoD is met. The fix
itself is low-risk and self-contained; the corpus completeness is the bigger, separate body of work.

On the PI's call, **`git merge --no-ff`** into `main` (local-first). NOTHING pushed/merged before
PI approval.

## 7. Red-before evidence (for the close-out)

All 5 new fixtures proven non-vacuous, **re-confirmed against the committed validator code (`87e2a49`,
unchanged by later doc commits)**:
sym-a/cov-a RED under blanket-strict mutation; sym-b/cov-b RED under blanket-lenient mutation;
fixture-c RED as `ModuleNotFoundError` pre-module. Green under v1.2.

Provenance note: a mid-session `git checkout` (reverting a transient mutation) wiped the uncommitted
v1.2 edits in `validator_cross_tile.py`; they were re-applied and re-verified. The mutation reds were
therefore **re-run against the committed sha** (not the wiped pre-checkout code) so they certify what
is actually committed. `lossless_clip.py` was never touched by the checkout (only
`validator_cross_tile.py` was), so fixture-c is unaffected. Lesson: never `git checkout` a file with
uncommitted work.
