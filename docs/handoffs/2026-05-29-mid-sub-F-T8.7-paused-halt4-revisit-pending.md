# Session handoff — mid sub-F, T8.7 paused (Halt 4 revisit pre-decided, execution pending) — 2026-05-29

> For the incoming agent: this is a resumable handoff. Assume no prior
> conversation memory. This document plus the referenced repo files are
> the entry point for completing Phase 1 sub-F from this paused state
> onward.

## Branch State

- Branch: `phase-1-sub-F-micro-tokenizer`
- Latest commit at handoff: `2562c89` `wip(sub_f): BP3 chunked-budget audit — Halt 4 REVISIT required (P99.9 +107 raw > 96 slack)`
- Worktree at handoff: clean.
- Do not push, create PRs, or merge. The human reviewer owns merge at sub-F close.

Chronological commit chain on sub-F branch (most-recent first; T8 sub-tasks 1-6 closed; T8.7 paused):

```text
2562c89 wip(sub_f): BP3 chunked-budget audit — Halt 4 REVISIT required (P99.9 +107 raw > 96 slack)
86f0c99 fix(sub_f): encoder chunking per §3.5 (T8.4 retrofit) + UNVERIFIED-comment sweep
9f9344f docs(sub_f): T8.7 plan + spec §13.1 — bref vertex position v2-scoped (NO v1 round-trip gate)
277d7f5 feat(sub_f): T8.6 encode_cell per-cell aggregator + empty-cell handling
efa6786 feat(sub_f): T8.5 BP7 sub-E boundary-contract reader (source-derived contract + fail-loud parse-boundary)
74ff03e fix(sub_f): T8.5 plan rewrite — source-derived contract (was: inferred-with-hardening)
acd666b docs(sub_f): T8.5 plan hardening — provisional-fixture tagging + fail-loud parse boundary
748291a feat(sub_f): T8.4 encoder 4-case grammar (A/B/C/D per §3.2)
0d4bc47 docs(sub_f): fix T8.8 step 8 inverted dependency (T5b is post-T8 follow-up, not no-op un-skip)
96e87fe feat(sub_f): T8.3 canonicalize_geometry + encoder helpers (BP5 contract; geom_type routing)
f2e7250 feat(sub_f): T8.2 cells.parquet pinned schema + write_cells_parquet
82fed5b fix(sub_f): update sentinel-inventory tests for post-4c4f880 structural-sentinel consumption
b913611 feat(sub_f): T8.1 vocab loader — 374 on-disk slots across BP1/BP2/structural/BP4/BP7
49a79d3 docs(sub_f): T8 writer dispatch-ready plan (8 sub-tasks; incorporates sentinel-inventory amendment)
4c4f880 fix(sub_f): sentinel_inventory — consume 509, 510 from reserved_v2_headroom for <feature>/<feature_end>
a0661ed feat(sub_f): T3c BP3 budget LOCKED — P99.9 + per-type floor calibration + token-anchored diagnostic with action contract (Halt 4 approved)
8b87d84 docs(sub_f): T3c Halt 4 report
25a0493 wip(sub_f): T3c pre-halt — BP3 budget surface (formula stage-4; Halt 4 pending)
7960a19 feat(sub_f): T3b stage-3 compound per-cell length sans cross-cell
ba04360 feat(sub_f): T3a joint P(feature_count, vertex_count|cell,type) on Singapore
2aed4e1 fix(sub_f): T5a Halt 5 follow-up — drop open-polyline direction DOF (cheap-to-keep)
e9c1831 feat(sub_f): T5a BP5 LOCKED — 4-DOF canonical form + banker's round (Halt 5 approved)
609e523 wip(sub_f): T5a pre-halt - BP5 verifications (Halt 5 pending)
d48889e docs(sub_f): record sub-E gap decision — T3c stage-4 code-inferred for v1
```

## Where Sub-F Stands

Closed and approved (all 7 reviewer halts):

- Task 1 / Halt 1: BP1 semantic vocab floor locked.
- Task 2 / Halt 2: BP2 encoder primitives locked.
- Task 3a / 3b / 3c / Halt 4: BP3 budget locked — **REVISIT pre-decided this session (see below).**
- Task 4 / Halt 3: BP4 unknown family + sentinel inventory locked.
- Task 5a / Halt 5: BP5 verification canonical form locked.
- Task 6 / Halt 6: BP6 six-axis version manifest locked.
- Task 7 / Halt 7: BP7 boundary-reference vocab locked.

T8 sub-tasks closed: T8.1 (vocab loader) → T8.2 (cells.parquet schema) → T8.3 (canonicalize_geometry) → T8.4 (encoder 4-case grammar) → T8.5 (BP7 sub-E boundary-contract reader, source-derived) → T8.6 (encode_cell aggregator). 6 of 8 sub-tasks done.

Sub-F state: 135/135 tests passing on the branch.

**Paused at T8.7 dispatch** because two cross-coupled issues surfaced and were pre-decided this session, with execution deferred for context hygiene:

1. **Encoder chunking defect (T8.4 retrofit) — FIXED at `86f0c99`.** Encoder silently truncated geometry segments > 32m per spec §3.5; surfaced by T8.7 dispatch aggregate stress test (L_∞ = 183m vs 4.8m Halt 2 lock). Fix landed; T8.4 tests updated (Case C/D shrunk to short segments; new chunking tests added for §3.5 example + parametric ceil distance verification). Stale-UNVERIFIED-sub-E-comment sweep bundled (lock-and-guards-travel-together with T8.5's source-derived reframing).
2. **BP3 budget shifted under chunking — Halt 4 REVISIT pre-decided as option 1, execution deferred.** New audit script `scripts/sub_f/audit_chunked_budget.py` (commit `2562c89`) computed per-feature tokens against actual segment distances on cached Singapore. P99.9 raw moved 5,792 → 5,899 (+107, overshoot the 96-token slack by 11); P99.9 padded moved 5,888 → 6,016 (+128, exactly one padding block above the locked budget). Per-type chunked inflation: roads +13.8%, buildings +3.4%, POIs +0%, base/landuse +26.5%.

## CRITICAL — Halt 4 Disposition (Pre-Decided)

**Reviewer ratified option 1: Re-lock at chunked P99.9 (5,899 raw / 6,016 padded).** Numbers are committed in `reports/sub_f_chunked_budget_audit.yaml`; do NOT re-derive. The decision was reasoned through this session (audit is conclusive; options 2 and 3 rejected for cheap-to-keep / FLOPs reasons). Next session executes the mechanical re-lock against the audited numbers.

**Rationale recorded for §13.1 entry (to land at next session):** Halt 4 lock at `configs/sub_f/sequence_length_analysis.yaml::lock.recheck_obligations` already had spirit-of-recheck obligations (revisit triggers when sub-E measured stage-4 differs from formula). The chunking formula wasn't a named trigger because it was undiscovered at Halt 4; the new audit IS a recheck-triggered revisit per the spirit of those obligations. Cost (+128 tokens padded, ~2% transformer FLOPs at attention) is trivial vs silent under-coverage on ~1% of road + base-heavy cells. Options 2 (stay at 5,888; accept higher drop) and 3 (move to chunked P99.99) explicitly rejected: option 2 is the named cheap-to-keep violation pattern we've been catching all session; option 3 overfits.

## Mechanical Re-Lock Steps (Next Session)

Execute in order against fresh context. Each step is small and independent enough to interleave verification.

### Step 1 — Update `configs/sub_f/sequence_length_analysis.yaml`

- `_status: LOCKED` stays. Add `lock.revisit_2026_05_29` block recording the chunking-cascade revisit.
- `lock.elbow_budget_raw_tokens`: 5792 → **5899**
- `lock.elbow_budget_padded_tokens`: 5888 → **6016**
- `lock.elbow_rationale` text: keep the marginal-cost-of-cut framing; append a note that the budget reflects the chunked encoder formula per spec §3.5 (commit `86f0c99`) and the audit per `reports/sub_f_chunked_budget_audit.yaml` (commit `2562c89`).
- `lock.retention_floors_per_type`: re-derived from the regenerated α drop report (Step 2). Expect drift slightly higher (fewer cells dropped at the higher budget). Old values: roads 99.34% / buildings 98.83% / POIs 90.18% / base 99.92%.
- `lock.formula_error_residual_risk.padding_slack_tokens`: recompute. New raw 5899; new padded 6016; new slack = 117 tokens (slight bump from 96). The stage-4 formula error magnitude stays at 3-7 tokens/cell; absorbed comfortably.
- `lock.long_cell_diagnostic.threshold_tokens`: 5632 → may shift OR stay; reviewer choice. The 2-padding-block-below-padded-budget anchor would put it at 6016 − 256 = **5760**. Diagnostic action contract stays.

### Step 2 — Regenerate α drop report against new budget

```bash
uv run python scripts/sub_f/compute_alpha_drop_report.py \
    --sub-c-region-dir data/processed/sub_c/2026-04-15.0/singapore/ \
    --budget-raw 5899 --budget-padded 6016 --label alpha_drop_at_p999_chunked
uv run python scripts/sub_f/compute_alpha_drop_report.py \
    --sub-c-region-dir data/processed/sub_c/2026-04-15.0/singapore/ \
    --budget-raw 6016 --budget-padded 6016 --label alpha_drop_at_p999_chunked_padded
```

**IMPORTANT:** the existing `compute_alpha_drop_report.py` uses the UNCHUNKED formula (`case_a_tokens(v, n_anchor) = 3 + n_anchor + 2*(v-1)`). For an accurate redo against the chunked budget, the script needs to be updated to use chunked per-feature costs (matching `audit_chunked_budget.py:_per_feature_chunked_tokens`). The cleanest path is to refactor: extract the chunked cost function into a shared module (e.g., `cfm.data.sub_f.token_cost.chunked_per_feature_tokens(coords, n_anchor)`) and import in both scripts. Otherwise the drop report compares chunked-budget against unchunked-actuals and the drop counts are wrong.

Replace the two existing drop-report YAMLs in `reports/`:
- `sub_f_task_3c_alpha_drop_at_p999.yaml` → kept for audit trail; add note in YAML body that this is the pre-chunking baseline
- `sub_f_task_3c_alpha_drop_at_p999_padded.yaml` → same
- Add: `sub_f_task_3c_alpha_drop_at_p999_chunked.yaml` + `..._chunked_padded.yaml`

### Step 3 — Recompute per-type floors at new budget

Per-type floors should be set at the natural coverage given the new budget (same category-error-correction rationale as the original Halt 4 lock). Expect drift slightly higher across the board:
- Roads: was 99.34% → expect ~99.5-99.7% (chunking inflates road tokens by 13.8%; more road cells fit under the higher budget)
- Buildings: was 98.83% → expect ~99.0% (modest 3.4% inflation)
- POIs: was 90.18% → expect ~90% (no chunking on points, but cells with POIs alongside roads/buildings may shift; recompute)
- Base/landuse: was 99.92% → expect ~99.95% (high inflation but high baseline retention)

Reviewer ratifies the recomputed numbers.

### Step 4 — Spec §13.1 entry recording the revisit

Add a row to `docs/superpowers/specs/2026-05-23-phase-1-sub-F-micro-tokenizer-design.md` §13.1:

| Title | Body |
|---|---|
| **Halt 4 revisit → encoder chunking discovered at T8.7 dispatch; BP3 budget +128 padded; new lock at 5,899 raw / 6,016 padded** | Surfaced when T8.7 aggregate stress test L_∞ = 183m revealed encoder chunking gap (spec §3.5). Fix at `86f0c99`; audit at `2562c89` showed P99.9 raw shifted 5,792 → 5,899 (+107 = +11 over 96-token slack). Per-type chunked inflation: roads +13.8%, buildings +3.4%, POIs +0%, base +26.5%. Spirit-of-recheck-obligations revisit per the existing Halt 4 lock's `recheck_obligations` block, not a new design. Re-lock numbers + per-type floors + new α drop report land in this revisit commit. Resolution rationale recorded: cost (+128 tokens, ~2% FLOPs) is trivial vs the silent under-coverage on ~1% of road + base-heavy cells that option 2 (stay at 5,888) would have caused (named cheap-to-keep violation pattern per session lesson). |

### Step 5 — Update `reports/2026-05-23-phase-1-sub-F-close-checklist.md`

Mark the new chunking-formula audit as discharged. Add line for "α drop report regenerated against chunked budget; per-type floors recomputed." Keep the existing real-data sub-E obligation (per T8.5).

### Step 6 — Commit re-lock atomically

```
git add configs/sub_f/sequence_length_analysis.yaml \
        reports/sub_f_task_3c_alpha_drop_at_p999_chunked*.yaml \
        docs/superpowers/specs/2026-05-23-phase-1-sub-F-micro-tokenizer-design.md \
        reports/2026-05-23-phase-1-sub-F-close-checklist.md \
        scripts/sub_f/compute_alpha_drop_report.py  # if refactored
        src/cfm/data/sub_f/token_cost.py             # if extracted

git commit -m "feat(sub_f): T3c BP3 budget RE-LOCKED — chunked P99.9 (5,899 raw / 6,016 padded) per Halt 4 revisit at T8.7 dispatch"
```

Lock-and-guards-travel-together: ensure any test asserting on the old `5792` / `5888` values (likely none — these were lock values, not test expectations — but `git grep -F "5792" tests/ ; git grep -F "5888" tests/` first).

## T8.7 + T8.8 Sequencing After Re-Lock

T8.7 plan is at commit `9f9344f` — bref vertex deferred to v2-scoped per §1.4 + §13.1 (ratified this session). The plan stands as written; dispatch against the re-locked budget context.

T8.7 dispatch checklist (after Step 6 above):
- Read `docs/superpowers/plans/2026-05-28-phase-1-sub-F-task-8-writer.md` Sub-task T8.7 section.
- Read §1.4 scope lock #1 + §13.1 the T8.7 plan-write bref deferral row.
- Confirm test_decoder.py asserts L_∞ <= 4.8m (Halt 2 lock — unchanged by Halt 4 revisit; 4.8m is BP2-side, budget is BP3-side).
- The 7 tests are: canonical_geojson byte-stable, open LineString round-trip, closed-LineString round-trip, canonicalize-vertex-preservation invariant, Polygon round-trip, aggregate L_∞ across 30 synthetic fixtures, NEGATIVE bref-vertex-not-asserted.
- Expected: 135 + 7 = 142 tests pass after T8.7 dispatch (135 current + 7 from decoder tests; minus subagent-discovers any other lurking issue).
- HALT posture: tolerance relaxation on 4.8m, geometry-type carve-out, OR extension of round-trip into bref vertex → STOP. Same as the prior dispatch prompt at `9f9344f`.

T8.8 dispatch checklist (after T8.7 closes):
- Per-tile orchestrator `pipeline_writer.py` + 4-case round-trip integration tests (most under `@pytest.mark.skip("awaiting sub-E cache regeneration")`).
- Close-checklist obligation: when sub-E lands, un-skip the integration test + walk `FIXTURE_SOURCE_DERIVED_INDEX` per T8.5's framing (residual real-data debt only — schema/sentinel facts are source-derived).

## Carry-Forwards Summary

- **T5b post-T8 obligation:** master plan T5b creates `tests/data/sub_f/test_per_axis_determinism.py` per spec §5.2 + §5.6 canonical-form coverage. Blocked on T8 (encoder) shipping; unblocked after T8.8. Close-checklist line tracks; dispatch as a separate sub-task post-T8.8. See `0d4bc47` commit (inverted-dependency fix for T8.8 step 8).
- **Sub-E real-data recheck:** T8.5 reader is source-derived against sub-E `writer.py`/`rotation.py`/`derivation.py`/`validator_inline.py`. Close-checklist obligations narrowed to: (a) first real sub-E read must succeed without `SubEContractViolation`, (b) T3c stage-4 empirical ratio spot-check, (c) motorway + MultiLineString-collapse spot-checks per §13.1 cascades #9 + B. NO fixture-by-fixture re-validation (per the source-derived framing).
- **BP7 docstring sweep:** completed at `86f0c99` — `encoder.py::_classify_feature_for_bref` + `boundary_contract.py` residual-debt paragraph rewritten to reflect T8.5's source-derived reality. No remaining "UNVERIFIED against real sub-E" comments in `src/cfm/data/sub_f/`.
- **§13.1 bref position v2 deferral:** landed at `9f9344f`. Spec §1.4 scope lock #1 + §3.8 case 4 "larger tolerance" reframed as v2-scoped per §12 #1; cell_extent/2 = 125m recorded as v1 imprecision bound, NOT as tested threshold.
- **Halt 4 revisit:** see Steps 1–6 above. Execute in Step-6 atomic commit.
- **§9.1-§9.5 line-number cite sweep:** still backlogged per `feedback_content_anchored_cites`.
- **Sub-E cache regeneration:** still absent (per `project_sub_e_cache_absent_t3c_code_inferred`). T8.5 reader works against synthetic fixtures + real-data integration deferred per close-checklist.

## New Memories This Session

Two new feedback memories surfaced from this session's halt patterns, both recurring failure modes:

1. **`feedback_decompose_verification_debt_before_inferring`** (T8.5 first-dispatch halt, 2026-05-28) — "verification debt" bundles absent-data debt (real; permits inference) and unread-source debt (Gate-6 violation hiding under a plausible label). Decompose before invoking `_INFERRED_X` constants. T8.5's plan inferred sub-E schema/dtypes/enum/sentinel from spec when sub-E source was in-repo. Subagent caught at pre-dispatch audit.
2. **`feedback_test_spec_not_just_plan`** (T8.7 chunking-defect halt, 2026-05-29) — when implementing against a locked spec, test cases must exercise spec corner cases (magnitude limits, alternation rules, fallthrough), not just plan happy-path. T8.4 shipped no-chunking because plan didn't include §3.5 chunking and synthetic 3-vertex small-segment tests didn't exercise it. T8.7 aggregate stress with real-data-scale inputs surfaced the gap.

The pair name a recurring structural pattern: **partial reading (of plan vs spec, or plan vs upstream source) ships past lock when tests verify the partial reading is self-consistent on its own examples.** Both memories cross-reference for the pattern.

Also saved earlier this session: `feedback_diagnostic_threshold_design` (Halt 4 ratification: optimize early-warning for earliest reliable warning, not tightest non-noise; every threshold needs an action contract), `feedback_lock_and_guards_travel_together` (T8.1 cascade: lock changes that ship without their guards are silent regressions), `feedback_content_anchored_cites` (Halt 5: line numbers shift, content survives).

## Authoritative Files (For Next Session)

- Spec: `docs/superpowers/specs/2026-05-23-phase-1-sub-F-micro-tokenizer-design.md`
- T8 plan: `docs/superpowers/plans/2026-05-28-phase-1-sub-F-task-8-writer.md`
- Master sub-F plan: `docs/superpowers/plans/2026-05-23-phase-1-sub-F-micro-tokenizer.md`
- Close-checklist: `reports/2026-05-23-phase-1-sub-F-close-checklist.md`
- Halt 4 lock YAML: `configs/sub_f/sequence_length_analysis.yaml`
- Halt 4 audit (chunked): `reports/sub_f_chunked_budget_audit.yaml` (commit `2562c89`)
- Halt 4 original report: `reports/2026-05-23-phase-1-sub-F-task-3c-halt.md`
- α drop report tooling: `scripts/sub_f/compute_alpha_drop_report.py` (needs chunked-cost refactor — see Step 2)
- Chunked-budget audit tooling: `scripts/sub_f/audit_chunked_budget.py`
- Protocol: `docs/protocols/sub-project-planning-protocol-v1.md`

## Non-Negotiable Discipline For The Next Session

- Stay on `phase-1-sub-F-micro-tokenizer`. No push, no PR, no merge.
- Re-lock execution per Steps 1–6; commit atomically. Lock-and-guards-travel-together.
- T8.7 dispatch posture unchanged from `9f9344f`: HALT on tolerance relaxation, geometry-type carve-out, or extension of round-trip gate into bref vertex.
- Audit numbers from `2562c89` are conclusive — do NOT re-derive. Execute against them.

End of handoff. Next session reads this file + `reports/sub_f_chunked_budget_audit.yaml` + spec/plan, executes Steps 1–6, then dispatches T8.7.
