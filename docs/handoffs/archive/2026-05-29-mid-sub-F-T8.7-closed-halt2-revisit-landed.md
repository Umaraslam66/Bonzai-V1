# Session handoff — mid sub-F, T8.7 CLOSED + Halt-2 revisit LANDED (Halt-4 revisit also landed this session) — 2026-05-29

> Resumable handoff. Assume no prior conversation memory. This document plus the
> referenced repo files are the entry point for completing Phase 1 sub-F from this
> state onward. Clean break point: 7 of 8 T8 sub-tasks closed; BOTH lock revisits
> (Halt 4 budget, Halt 2 round-trip) absorbed.

## Branch State

- Branch: `phase-1-sub-F-micro-tokenizer`
- HEAD: `9a7d347` `feat(sub_f): BP2 Halt-2 RE-LOCKED — direction 48->360 (1° bins) + T8.7 decoder round-trip gate`
- Worktree: clean.
- Do NOT push, PR, or merge. Human reviewer owns merge at sub-F close. No new branches.

Commit chain this session (most-recent first):

```text
9a7d347 feat(sub_f): BP2 Halt-2 RE-LOCKED — direction 48->360 (1° bins) + T8.7 decoder round-trip gate
dac8e29 wip(sub_f): Halt 2 re-anchor scoped — also regresses angle (2.2x); more-directions is the only no-regression fix
b9522cc wip(sub_f): Halt 2 pre-lock checks — error-feedback DISQUALIFIED by angle regression
625df46 wip(sub_f): Halt 2 round-trip revisit scoping — error-feedback bounds L_inf to p99.9=4.1m at ZERO lock cost
c1eb2a1 feat(sub_f): T3c BP3 budget RE-LOCKED — chunked P99.9 (5,899 raw / 6,016 padded) per Halt 4 revisit
662721d docs(handoff): tighten 5 items per reviewer read (prior handoff)
```

## T8 Progress

- **T8.1–T8.7 CLOSED.** T8.1 vocab loader, T8.2 cells.parquet schema, T8.3 canonicalize_geometry, T8.4 encoder 4-case grammar (+ §3.5 chunking retrofit), T8.5 BP7 sub-E boundary-contract reader (source-derived), T8.6 encode_cell aggregator, **T8.7 decoder + canonical-GeoJSON round-trip gate (landed at 9a7d347).**
- **Remaining:** T8.8 (per-tile orchestrator), T5b (per-axis determinism suite, post-T8), close-checklist obligations.
- Test state: **164 sub-F tests pass; 716 full-suite pass** (21 slow-deselected, 1 pre-existing unrelated xfail). ruff clean on all changed files. (`src/cfm/data/sub_f/manifest.py` has 2 PRE-EXISTING ruff issues — line 9 import, line 48 B905 zip-strict — untouched this session, not in any commit here; fix opportunistically when you next touch manifest.py.)

## Halt 2 revisit (LANDED 9a7d347) — BP2 direction re-locked 48→360

T8.7's decoder round-trip gate exposed that the BP2 round-trip thresholds did not
hold on real data (`feedback_sample_regime_blind_locks`): 4.8m/7.5° were the p95 of
a 1,000-polyline sample that under-represented the real 862k distribution (real
honest position p95=8.8m at N=48), the chunking fix (86f0c99) unmasked it, and 7.5°
was merely the N=48 bin width. Three mechanisms were scoped on real data
(`reports/sub_f_halt2_revisit_scoping.md` + `sub_f_halt2_*_scoping.yaml`):
error-feedback (angle catastrophic corners 10×) and re-anchor (2.2× + a 3rd BP3
budget pass) each REGRESS the angle axis because they restructure relative vertex
placement; **only more-directions improves both axes.** Re-lock: `direction_count`
48→360 (1° bins); position gate **p99.9 ≤ 4.8m** (measured 3.7m via actual
encoder+decoder on real data) and angle gate **p95 ≤ 4.0°** (measured 3.0°), both
RE-DERIVED FRESH (not inherited from the weak sample-p95). The ~14m position max is
documented v1 known-loss (accumulation floor; continuous, NOT the categorical
right-angle known-loss). **ID relocation (append-safe):** 360 contiguous slots
don't fit at base 396 (magnitude 444-508 + structural 509-510 follow), so direction
relocated to **511-870** (front of reserved_v2_headroom, structural-sentinel
precedent); old **396-443 RETIRED to `direction_v1_deprecated`** (not reused —
reusing would reassign locked IDs); magnitude/structural/anchor/BP7 unchanged;
reserved 989→629; on-disk vocab 374→686. **BP3 budget UNAFFECTED** (tokens-per-pair
unchanged; pinned via `test_token_cost.py`). Lock files: `proposed_lock.revisit_2026_05_29`
in `configs/sub_f/encoding_primitives.yaml`; `direction_relocation_2026_05_29` in
`configs/sub_f/sentinel_inventory.yaml`; spec §13.1 Halt-2-revisit row.

NOTE on the angle gate: it is **NOT unit-tested** — a uniformly-rotated synthetic
rectangle preserves exact-90° corners at every bin width (false assurance; see
`feedback_gate_must_distinguish_regimes`). It binds via the YAML value + the
real-data measurement + a close-checklist re-measure obligation, exactly as the
original 7.5° did. The position gate DOES bind synthetically (`test_decoder.py`
aggregate failed 25.8m at N=48, passes 3.4m at N=360).

## Halt 4 revisit (LANDED c1eb2a1, earlier this session) — BP3 budget re-locked

Encoder §3.5 magnitude chunking (discovered missing at T8.7 dispatch, fixed 86f0c99)
shifted the BP3 budget. RE-LOCKED at chunked P99.9 = **5,899 raw / 6,016 padded**
(`configs/sub_f/sequence_length_analysis.yaml lock.revisit_2026_05_29`). Per-type
floors recomputed from the chunked PADDED-budget α cut — all drifted up/flat (roads
0.9934→0.9936, buildings 0.9883→0.9889, POIs 0.9018→0.9027, base 0.9992 unchanged).
Long-cell diagnostic re-anchored 5,632→5,760; padding slack 96→117. Per-feature cost
extracted to pinned `cfm.data.sub_f.token_cost`; emitter-faithful re-verification
(`scripts/sub_f/verify_chunked_budget_p999.py`) confirmed the padded budget holds
under per-part Multi* costing. (The Halt-2 N=360 widening does NOT touch this — direction
count doesn't change tokens-per-pair.)

## Memories created / refined this session

- **`feedback_sample_regime_blind_locks`** (NEW + refined) — a lock validated on a
  sample that never exercised a regime stops holding once a downstream fix makes the
  regime reachable; plus the metric-mismatch dimension (derived p95, enforced max)
  and "don't inherit a weak statistic when re-affirming." Meta-pattern over T8.5 /
  T8.7-chunking / Halt-2-4.8m.
- **`feedback_characterize_before_recommend`** (NEW) — don't recommend an option
  while a live alternative is unmeasured; scope ALL live options (especially the only
  one with a unique property) before recommending. Residual checks are PRE-lock.
- **`feedback_forced_precedent_tell`** (NEW) — reaching for an accepted precedent to
  justify a new decision is a tell the justification is thin; verify the defect CLASS
  matches (categorical ≠ continuous ≠ deferral). Right-angle known-loss was reached
  for twice for unrelated decisions.
- **`feedback_gate_must_distinguish_regimes`** (NEW) — a guard that passes identically
  in the broken AND fixed regime guards nothing; confirm it FAILS in the regime it
  targets, else bind via the real-data measurement. Worked example: the sub-F angle
  synthetic test (90° divides every bin width → passed at N=48 broken and N=360 fixed).
- **`feedback_test_spec_not_just_plan`** (refined) — Halt-2 added as its third worked
  example; cross-links to the meta-pattern above.

Prior-session memories still in force: `feedback_decompose_verification_debt_before_inferring`,
`feedback_diagnostic_threshold_design`, `feedback_lock_and_guards_travel_together`,
`feedback_content_anchored_cites`.

## T8.8 dispatch posture (NEXT — fresh context recommended)

- Files: `src/cfm/data/sub_f/pipeline_writer.py` + `tests/data/sub_f/test_pipeline_writer.py`.
- Per-tile orchestrator (sub-E precedent: writer → inline validator → cross-tile
  validator → `_SUCCESS` validate-then-touch; halt-on-validator-fail) + 4-case
  round-trip integration tests, MOST under `@pytest.mark.skip("awaiting sub-E cache
  regeneration")`. Plan: `docs/superpowers/plans/2026-05-28-phase-1-sub-F-task-8-writer.md`
  Sub-task T8.8 (note `0d4bc47` fixed the inverted T5b dependency — T5b is a post-T8.8
  follow-up, not a T8.8 no-op un-skip).
- **Risk profile is LOWER than T8.5–T8.7:** T8.8 composes already-verified sub-F-internal
  pieces (encoder, decoder, validators, manifest) — there is no external upstream
  authority to read and no locked threshold to re-derive. The recurring session failure
  mode (`feedback_sample_regime_blind_locks` / partial-reading-of-upstream) is unlikely
  to apply. Keep the same halt posture, but expect it to be quieter.
- Standard halt posture still holds: HALT on any locked-value change, any "weaken the
  assertion to pass," any new external-contract inference.

## Carry-forwards

- **T5b post-T8 obligation:** create `tests/data/sub_f/test_per_axis_determinism.py` per
  master plan T5b + spec §5.2/§5.6 (5 canonicalization adversarial tests). Unblocked
  after T8.8 ships the encoder pipeline. Run under `PYTHONHASHSEED=random` across cold
  invocations (`feedback_pythonhashseed_dict_iteration_test`).
- **Close-checklist** (`reports/2026-05-23-phase-1-sub-F-close-checklist.md`) open items:
  (a) real-data round-trip re-measure when running real cache / first Leonardo run —
  position honest p99.9 ≤ 4.8m AND right-angle-corner post-deviation p95 ≤ 4.0° (the
  angle gate has no synthetic unit test); (b) T8.5 sub-E first-real-data read without
  `SubEContractViolation` + T3c stage-4 empirical ratio + motorway / MultiLineString
  spot-checks; (c) sub-E measured-stage-4 recheck (still the §7.2 formula; sub-E cache
  absent per `project_sub_e_cache_absent_t3c_code_inferred`); (d) pipeline orchestrator
  must invoke the α drop report at `--budget-raw=5760 --budget-padded=6016` per run.
- **Frozen analysis scripts** (NOT live tools; retained as evidence): `scripts/sub_f/`
  `scope_halt2_roundtrip.py`, `scope_halt2_fixes.py`, `scope_halt2_direction_sweep.py`,
  `scope_halt2_reanchor.py`, `scope_halt2_residual_checks.py`, `verify_chunked_budget_p999.py`,
  `audit_chunked_budget.py`. Some import encoder constants that moved at the N=360 re-lock
  (e.g. hardcoded `DIR_COUNT=48`) — do NOT re-run them post-revisit; they document the
  pre-/at-revisit analysis. The committed YAML reports are the durable record.
- **§9.1–§9.5 line-number cite sweep:** still backlogged (`feedback_content_anchored_cites`).

## Authoritative files

- Spec: `docs/superpowers/specs/2026-05-23-phase-1-sub-F-micro-tokenizer-design.md`
- T8 plan: `docs/superpowers/plans/2026-05-28-phase-1-sub-F-task-8-writer.md`
- BP2 lock: `configs/sub_f/encoding_primitives.yaml` (proposed_lock + revisit) + `sentinel_inventory.yaml`
- BP3 lock: `configs/sub_f/sequence_length_analysis.yaml`
- Halt-2 scoping: `reports/sub_f_halt2_revisit_scoping.md` (+ `sub_f_halt2_*_scoping.yaml`)
- Halt-4 audit: `reports/sub_f_chunked_budget_audit.yaml`
- Close-checklist: `reports/2026-05-23-phase-1-sub-F-close-checklist.md`
- Encoder/decoder/vocab: `src/cfm/data/sub_f/{encoder,decoder,vocab,token_cost}.py`

End of handoff. Next session: read this + the spec/plan + the BP2/BP3 lock YAMLs, then
dispatch T8.8 against fresh context.
