# Phase 1 Sub-F Close Checklist

Running obligations to verify before the Task 15 sub-F handoff commit.

- [x] **DISCHARGED 2026-05-29 — Halt 2 revisit (round-trip thresholds).** T8.7's decoder round-trip gate exposed that the BP2 thresholds (4.8m position / 7.5° angle) were the p95 of a 1,000-polyline sample that under-represented real data (real honest position p95 = 8.8m at N=48). RE-LOCKED: direction widened 48→360 (1° bins), the only mechanism that improves both axes without regressing the angle (error-feedback + re-anchor scoped and rejected — both regress angle). Thresholds re-derived FRESH: position **p99.9 ≤ 4.8m** (measured 3.7m via actual encoder+decoder on real data), angle **p95 ≤ 4.0°** (measured 3.0°). Direction block relocated append-safely 396-443→511-870 (`sentinel_inventory.yaml`); old block retired; budget unaffected; on-disk vocab 374→686. Full scoping: `reports/sub_f_halt2_revisit_scoping.md`; spec §13.1.
- [ ] **Real-data round-trip re-measure (REQUIRED before sub-F-close / first training run).** The position gate has a binding synthetic proxy (`test_decoder.py` aggregate max ≤ 4.8m), but the ANGLE gate (right-angle-corner post-deviation p95 ≤ 4.0°) is NOT unit-tested — its failure mode is data-dependent and synthetic right angles are preserved at every N (false assurance). Re-measure on the full real cached Singapore (and Sweden + Sri Lanka when available): (a) position honest round-trip p99.9 ≤ 4.8m (currently 3.7m on a 1-in-5 sample via the actual encoder+decoder); (b) right-angle-corner post-deviation p95 ≤ 4.0° (measured 3.0° at N=360 in `scripts/sub_f/scope_halt2_residual_checks.py`). If either regresses, surface as a Halt-2-revisit-2 candidate. The ~14m position max is documented v1 known-loss (accumulation floor; not a defect of N). The pytest re-measure now lives in `tests/data/sub_f/test_singapore_integration.py::test_singapore_roundtrip_position_and_angle` (T13; `-m slow`, fail-loud on absent cache), which uses the REAL locked `encode_feature`/`decode_feature` (N=360) + `token_cost.chunked_segment_pairs`. **DO NOT reuse `scripts/sub_f/scope_halt2_residual_checks.py` or `scope_halt2_roundtrip.py` for this re-measure — both are hardcoded to the OLD 48-bin direction count (`BIN_DEG_48` / `DIR_COUNT=48`) and are stale post-N=360-relock (T13 Step-0 catch, 2026-05-30); a script-based re-measure must update them to N=360 first.**
- [x] **DISCHARGED 2026-05-29 — Halt 4 chunking-formula revisit.** Encoder magnitude-chunking (spec §3.5) was discovered missing at T8.7 dispatch (fix 86f0c99) and shifted the BP3 budget surface. BP3 budget RE-LOCKED at chunked P99.9 = 5,899 raw / 6,016 padded (`configs/sub_f/sequence_length_analysis.yaml lock.revisit_2026_05_29`). α drop report regenerated against the chunked budget (`reports/sub_f_task_3c_alpha_drop_at_p999_chunked.yaml` + `..._chunked_padded.yaml`); per-type floors recomputed from the chunked padded cut (roads 0.9936 / buildings 0.9889 / POIs 0.9027 / base 0.9992). Per-feature cost extracted to `cfm.data.sub_f.token_cost` and pinned against the encoder (`tests/data/sub_f/test_token_cost.py`); emitter-faithful re-verification (`scripts/sub_f/verify_chunked_budget_p999.py`) confirms padded budget holds under per-part Multi* costing. Long-cell diagnostic mechanically re-anchored 5,632→5,760. NOTE: this discharges only the CHUNKING revisit; the sub-E measured-stage-4 recheck obligation (below) remains OPEN — sub-E cache is still absent and stage-4 remains the §7.2 formula.
- [ ] After Task 7 locks BP7 boundary-reference vocab, add the boundary-ref vocab source to the sub-F region manifest `vocab_sources`, recompute final complete `manifest_sha256`, and clear/update `vocab_sources_status` from `partial_pending_bp7`.
- [ ] In the sub-F-close handoff, document that sub-F SOURCE derivation hard-depends on sub-C manifest field names `sub_c_schema_version` and `initial_extraction.commit_sha`; future sub-C schema bumps or renames must update `src/cfm/data/sub_f/versions.py`.
- [ ] In the sub-F-close handoff, record SOURCE `VersionRef.value` canonical format: `overture=<release>;subc_schema=<ver>;subc_commit=<full_sha>`.
- [ ] In the sub-F-close handoff, document sub-E-v2 candidate: refine highway tiering in sub-E grouping map (`motorway` -> MAJOR; decide non-vehicular way handling). sub-F BP7 inherits this through architecture (b).
- [ ] In the sub-F-close handoff, document sub-E-v2 candidate: evaluate whether same-edge MultiLineString / multi-part road crossings need richer than one-class-per-edge representation.
- [ ] In the sub-F-close handoff, document cross-sub-project coupling: sub-F BP7 classes are 100% sub-E-derived; sub-E boundary_contract schema or semantic changes propagate directly to sub-F BP7 output and training-scaffold must know this.
- [ ] When sub-E output is regenerated or restored, spot-check actual `boundary_contract.parquet` emission for motorway-only and same-edge MultiLineString edges against Task 7's code-inferred behavior. If actual output diverges, update BP7 limitation docs; the BP7 lock remains faithful-passthrough.
- [ ] **T8.5 BP7 reader first-real-data integration (REQUIRED when sub-E regenerates).** `src/cfm/data/sub_f/boundary_contract.py` is built from a SOURCE-DERIVED contract (see `_SUB_E_CONTRACT` constant for file:line citations into sub-E writer/rotation/derivation/validator_inline). Schema, dtypes, enum values, sentinel rules, hierarchy, join shape — all source-knowable. The reader raises `SubEContractViolation` at the parse boundary on any drift from the source-derived constants. Residual real-data obligation narrowed (per `feedback_decompose_verification_debt_before_inferring` — caught at T8.5 first dispatch 2026-05-28): (a) **first real sub-E read must succeed without `SubEContractViolation`**; if it raises, sub-E source has drifted since plan-write and the consumer needs updating (lock-and-guards backflow into sub-E). (b) **Spot-check T3c stage-4 empirical ratio**: count active road-class edges per cell in real sub-E and compare to the `~0.7 tokens/non-empty-cell` formula assumption in `configs/sub_f/sequence_length_analysis.yaml` (see also the existing T3c close-checklist obligation). (c) **One real-data BP7 emission spot-check per spec §13.1 cascades #9 + B**: verify at least one motorway-only edge surfaces as MINOR (cascade #9 documents this is source-true via `boundary_vocab.yaml` MAJOR_ROAD list + `derivation.py:85` fallthrough — but worth one empirical confirmation), and verify at least one same-edge MultiLineString collapse case emits the hierarchy-winning class. NO fixture-by-fixture re-validation needed — the synthetic fixtures test source-derived behavior and are self-consistent by construction.
- [ ] When sub-E output is regenerated or restored, recompute T3c stage-4 cross-cell overhead empirically against `boundary_contract.parquet` and compare to the code-inferred `~0.7 tokens/cell` figure in `configs/sub_f/sequence_length_analysis.yaml`. If empirical value diverges materially from the §7.2 estimate (>2 tokens/cell systematic mean), surface as a Halt-4 revisit candidate (budget surface elbow + per-type floors may shift). 2026-05-28 decision: sub-F-v1 ships with code-inferred stage-4 because sub-E cache is absent locally — see `project_sub_e_cache_absent_t3c_code_inferred` memory.
- [x] **DISCHARGED 2026-05-30 (T11) — alpha-drop warning-band wiring.** Pipeline orchestrator (`src/cfm/data/sub_f/pipeline.py::derive_region`) invokes the warning-band diagnostic after a successful region derive via `scripts/sub_f/compute_alpha_drop_report.py::run_alpha_drop_report` (thin importable entrypoint added; the script's `main` also calls it) at the RE-LOCK budgets `--budget-raw=5760 --budget-padded=6016` (commit c1eb2a1; NOT the original 5792/5888) and logs the warning-band cell count + per-type composition into the region run report (a `reports/sub_f_task_3c_warning_band_<region>.yaml` artifact + a log line). The diagnostic is advisory and runs AFTER `_SUCCESS`, so a report glitch never invalidates an already-passed region (failures are logged, not raised). Tested in `tests/data/sub_f/test_pipeline.py::test_derive_region_emits_alpha_drop_warning_band_report` (asserts budgets 5760/6016) + `::test_alpha_drop_report_entrypoint_returns_report_dict`. OPEN OBLIGATION (still tracked): the revisit triggers (warning-band count >14 OR >0.1% of cells OR sub-E measured stage-4 >2 tokens/cell systematically OR 3 consecutive runs monotonic increase) per Halt 4 lock `long_cell_diagnostic.action_contract` (RE-LOCKED 2026-05-29 `revisit_2026_05_29`) must be EVALUATED against real Singapore output once caches regenerate — the wiring emits the numbers but the trigger comparison + any resulting action is a human/follow-up step.
- [ ] **T11 real-region derive (REQUIRED when sub-C/sub-D/sub-E caches regenerate).** Run (under `-m slow`, fail-loud on absent cache) `tests/data/sub_f/test_singapore_integration.py::test_singapore_end_to_end_derive` (T13-consolidated home); run `derive_region` against the real cached Singapore region (real sub-C features, sub-D + sub-E `_SUCCESS` markers + boundary contracts) and assert `_SUCCESS` + `manifest.yaml` + per-tile `cells.parquet`/`provenance.yaml` with distinct tile shas, then run the BP7 composite via `validate_cross_tile` on the real output. The synthetic multi-tile derive tests (halt-on-inline/cross-tile, restartability byte-identity, alpha-drop wiring) ship now; the real-region derive is gated on all three caches. This is the orchestrator layer of the BP7 verification debt inherited from T7/T8/T10.
- [ ] When sub-E lands and multi-region (Sweden + Sri Lanka) data exists, re-evaluate the β-upgrade case for the training-scaffold sub-project. Halt 4 ratification surfaced that the current Singapore α drop set is dominated by cells 15–52% over budget (min 5,913 / median 6,719 / max 8,966 tokens) — β within-cell tail-drop recovers little because dropped cells are far over budget, not hovering at the elbow. β-upgrade is lower priority than the original Halt 4 implementer surface implied. Honest re-measure required before scoping.
- [ ] **T5b per-axis determinism suite (REQUIRED before sub-F-close).** Create `tests/data/sub_f/test_per_axis_determinism.py` per master plan Task 5b. Tests cover each row of spec §5.2 discipline table + the 5 canonicalization adversarial tests for §5.6 (open-polyline preserve, closed-LineString preserve, ring start-vertex rotation, ring winding correction, multi-part order with tiebreak). T5b unblocked by T8 (encoder shipped at T8.8); the original T8 plan assumed T5b had already run and only needed a no-op un-skip, which was an inverted dependency. Per spec §5.5 the per-axis determinism suite IS the durable contract artifact for sub-F determinism. Run under `PYTHONHASHSEED=random` across cold pytest invocations per `feedback_pythonhashseed_dict_iteration_test`. Owner: dispatch as a fresh subagent post-T8.8.
- [ ] OPEN SCHEDULE DECISION (Phase-1 level, not sub-F-internal): sub-E MINOR-default under-tiers motorway as MINOR. Accepted for sub-F-v1 faithful-passthrough. Decision needed: does sub-E tiering fix (`motorway` -> MAJOR) land before first Leonardo training run, or is degraded arterial tiering acceptable for first-run validation? Owner: human reviewer. Inherited by training-scaffold sub-project.
- [ ] Run (under `-m slow`, fail-loud on absent cache) `tests/data/sub_f/test_singapore_integration.py::test_singapore_encode_layer_real_sub_e` (T13-consolidated home) when sub-E cache regenerates — first-real-read without `SubEContractViolation` + per-tile inline well-formedness. (Grammar B/C/D cross-reference + the BP7 four-test composite are covered by `::test_singapore_cross_tile_composite`.) This is the T8 layer of the BP7 verification debt that was inherited from T7.
- [x] **DISCHARGED 2026-05-29 — T5b per-axis determinism suite.** Created `tests/data/sub_f/test_per_axis_determinism.py`: 25 tests covering §5.2 per-axis discipline (quantize_coord_m banker's round, direction_bin 360-count tie-break, Gate-6 sub-C/sub-E sort keys), §5.6 canonicalization adversarial DOFs (5a/5f open+closed LineString, 5b ring rotation, 5c winding, 5d multi-part sort, 5e tiebreak), and Risk-2 determinism integration (same-process twice, canonical-equivalence polygon rotation, cold-subprocess byte-identity, cross-PYTHONHASHSEED vocab order). 1 real-Singapore §5.5 skip stub added — gated on sub-E cache regeneration (see next item).
- [ ] De-duplicate the sub-E 144-row test fixture builder: `_make_full_tile_contract_rows` (test_pipeline_writer.py AND test_per_axis_determinism.py) duplicates `_make_full_tile_rows` (test_boundary_contract.py). Extract to a shared `tests/data/sub_f/conftest.py` fixture so a sub-E schema bump can't leave one copy stale (lock-and-guards-travel-together). **Note: test_per_axis_determinism.py now has a third copy — dedup is now higher priority.**
- [ ] §5.5 real-Singapore determinism: run (under `-m slow`, fail-loud on absent cache) `tests/data/sub_f/test_singapore_integration.py::{test_singapore_determinism_same_process, test_singapore_determinism_fresh_process, test_singapore_roundtrip_position_and_angle}` (T13-consolidated home) when the sub-E cache regenerates — same-process + fresh-process cells.parquet byte-identity + the position/angle round-trip re-measure (see #6) against the real cached Singapore tiles per spec §5.5 (the synthetic-tile determinism tests ship now; the real-cache leg is gated on sub-E).
- [ ] **T10 real-region BP7 composite (REQUIRED when sub-E + sub-F caches regenerate).** Run (under `-m slow`, fail-loud on absent cache) `tests/data/sub_f/test_singapore_integration.py::test_singapore_cross_tile_composite` (T13-consolidated home); run `validate_cross_tile(sub_f_region_dir, sub_e_region_dir)` over the real cached Singapore region — cross-reference / symmetry / non-road / coverage + version-manifest consistency against real cached tiles. The synthetic per-leg negatives ship now (rule-isolation verified by leg-neutering: each negative fails iff its target leg is disabled); the real-region composite is gated on both caches.
- [ ] **Motorway tiering carry-forward (do NOT let the green T10 BP7 tests imply this is validated).** sub-E tiers `motorway` as MINOR for v1 (a SCOPED accept, not a data-validated decision). T10's cross-reference + class-map tests pass whenever encoder and contract AGREE on MINOR-for-motorway — they do NOT validate that MINOR is semantically correct for motorways. Add a real-data spot-check that motorway/trunk arterials are tiered acceptably (sub-E `motorway`→MINOR is a scoped v1 accept, not validated); decide before the first Leonardo run whether the sub-E tiering fix (`motorway`→MAJOR) lands first. Owner: human reviewer.
- [ ] Cross-tile-boundary bref symmetry (T10 deferral trigger): T10's symmetry leg validates WITHIN-tile (112 INTERNAL edges) only — correct for v1 because sub-E marks tile-boundary EXTERNAL edges as scope=non-active→NONE, so the encoder emits no bref there. TRIGGER: if sub-E ever makes external/tile-boundary edges active (e.g. the motorway→MAJOR tiering fix routes an arterial across a tile boundary), cross-tile-boundary bref symmetry becomes required and T10 must be extended (needs the sub-D inter-tile neighbour graph). Tie this to the sub-E motorway-tiering decision above.
